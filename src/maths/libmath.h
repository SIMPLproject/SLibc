#ifndef MATH_H
#define MATH_H

#include <stdio.h>
#include "matrix.h"

double log(double x);
double exp(double x);
double pow(double x, double y);
double fabs(double x);

float sqrt(float x);

int abs(int x);

#endif /* MATH_H */
