#include <sys/cdefs.h>


void _dl_static_self_relocation() {
	// # setup
	// get the dl link_map
	// set ut the l_addr to elf machine addr
	// set the l_ld to the dynamic addr
	// now if the dynamic part is in readonly
	//
	//# real relocation
	// get elf dynamic info
	// relocate
	//
	// debug stuff
	
}
